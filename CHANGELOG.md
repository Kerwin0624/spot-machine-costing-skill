# Changelog

所有版本变更在此按 SemVer 倒序记录。

## 0.1.4 - 2026-06-26

### Added

- 新增「新增行格式规则」：新增记录时仅 `Item` / 序号 / 编号列居中且加粗。

### Changed

- 除 `Item` / 序号 / 编号列外，本次写入的输入字段、拆分字段、公式字段、备注字段统一左对齐、正常字重，与主表既有数据列风格保持一致。

### Fixed

- 增加写后样式自检要求：若非 Item 列被误设为居中或加粗，应立即修正。
- 清理 README 调用示例中的真实交易方名称，改用占位公司名。

## 0.1.3 - 2026-06-25

### Added

- `README.md` 中新增「v0.1.3 变更速览」段落，集中展示接口匹配优先 + 品牌作为匹配条件 + 价格不硬编码。
- `SKILL.md` §5 标注「v0.1.3 起变更」，显式列出接口匹配 → 品牌匹配 → 通用 fallback 的优先级。

### Changed

- **数据盘接口匹配优先级调整**：
  - 此前按品牌选价库（v0.1.2 及更早）。
  - v0.1.3 起改为「**接口匹配先行 → 品牌匹配次之 → 通用 fallback**」。
- **品牌是匹配条件之一，不是覆盖项**：
  - 同一接口下不同品牌的机器，命中价库里该接口对应的品牌专属行；找不到品牌专属行时再回落该接口的通用行。
  - 超微 / 广达价格相对偏高是因为其数据盘使用 `E1.S` 接口，不是「E1.S 覆盖所有品牌」；华硕等使用 `U.2` 接口的机器仍按 `U.2` 通用行计入。
- **价格不硬编码**：在 SKILL.md / README.md 核心原则、§5、失败处理、贡献与变更中明确「所有金额来自 `标准单价database`，不在文档中硬编码」。
- **覆写安全**：在核心原则和失败处理中明确「覆写现有数据（数量 / 单价 / 公式值）必须先告知用户并等待确认」。

### Fixed

- 修正「E1.S 覆盖所有品牌」的过强措辞，替换为「接口命中后再按品牌匹配」。
- 移除文档中所有硬编码金额示例（保留价库结构与单位说明），改为引用价库。
- 修正 `Summary` Item 17（广达品牌、`E1.S` 数据盘）历史单价误判为通用规则的问题，由用户确认后更正。

### Compatibility

- 对 `U.2` 接口、品牌不在价库专属规则内的机器：行为不变。
- 对 `E1.S` 接口的现有行：若仍按 v0.1.2 填的旧单价，需要用户确认后批量更正。

## 0.1.2 - 2026-06-24

- 明确日常调用时默认在主表底部新增一行处理新机器。
- 明确 item / 序号字段按现有连续规则自动计数延续。
- 只有用户明确指定某一行时，才填写或覆盖指定行。

## 0.1.1 - 2026-06-24

- 扩充 README，补充安装方式、表格结构、调用示例、执行流程、配置摘要格式、成本拆分逻辑和失败处理。

## 0.1.0 - 2026-06-24

- 初始公开版。
- 支持按表头语义动态定位字段。
- 支持配置摘要识别、标准价库读取、成本拆分、公式补齐和自动核对流程。
- 删除所有私有表格链接、组织身份和内部业务上下文。
